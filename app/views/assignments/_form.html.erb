<%= form_with(model: assignment, local: true) do |form| %>
  <% if assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(assignment.errors.count, "error") %> prohibited this assignment from being saved:</h2>

      <ul>
      <% assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :job %>
    <%= form.select :job_id, options_for_select(Job.pluck(:name,:id)) %>
  </div>

  <div class="field">
    <%= form.label :boat %>
    <%= form.select :boat_id, options_for_select(Boat.pluck(:name,:id)) %>
  </div>

  <div class="field">
    <%= form.label :containers %>
    <%= form.number_field :containers, id: :assignment_containers %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
