<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :boat %>
    <%= form.text_field :boat, id: :user_boat %>
  </div>

  <div class="field">
    <%= form.label :job %>
    <%= form.text_field :job, id: :user_job %>
  </div>

  <div class="field">
    <%= form.label :containers %>
    <%= form.number_field :containers, id: :user_containers %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
